const bcrypt = require('bcryptjs');
const crypto = require('crypto');
const User = require('../../models/User.model');
const { loginSchema, registerSchema, forgotPasswordSchema, resetPasswordSchema } = require('../../validators/client/auth.validator');
const emailHelper = require('../../helpers/email');
const token = require('../../helpers/token');
const AUDIENCE='client';
const sessionOf=(r,req)=>({tokenHash:token.hashToken(r.token),jti:r.jti,familyId:r.familyId,audience:AUDIENCE,expiresAt:r.expiresAt,userAgent:String(req.get('user-agent')||'').slice(0,300),ip:req.ip});
exports.register=async(req,res,next)=>{try{const {email,password,name,phone}=registerSchema.parse(req.body);if(await User.exists({email}))return res.status(400).json({success:false,error:{code:'EMAIL_EXISTS',message:'Email đã được sử dụng'}});const user=await User.create({name,email,phone,passwordHash:await bcrypt.hash(password,10),role:'customer'});return res.status(201).json({success:true,data:token.serializeUser(user)});}catch(e){if(e.name==='ZodError')return res.status(400).json({success:false,error:{code:'VALIDATION_ERROR',message:e.errors[0].message}});return next(e);}};
exports.login=async(req,res,next)=>{try{const {email,password}=loginSchema.parse(req.body);const user=await User.findOne({email}).select('+passwordHash +refreshSessions');if(!user||!(await bcrypt.compare(password,user.passwordHash)))return res.status(401).json({success:false,error:{code:'INVALID_CREDENTIALS',message:'Email hoặc mật khẩu không đúng'}});if(user.status!=='active')return res.status(403).json({success:false,error:{code:'ACCOUNT_LOCKED',message:'Tài khoản đã bị khóa'}});const refresh=token.createRefreshToken(user,AUDIENCE);user.refreshSessions=[...(user.refreshSessions||[]).filter(s=>s.expiresAt>Date.now()),sessionOf(refresh,req)].slice(-5);await user.save();token.setRefreshCookie(res,AUDIENCE,refresh.token);return res.json({success:true,data:{accessToken:token.signAccessToken(user,AUDIENCE),user:token.serializeUser(user)}});}catch(e){if(e.name==='ZodError')return res.status(400).json({success:false,error:{code:'VALIDATION_ERROR',message:e.errors[0].message}});return next(e);}};
exports.getMe=(req,res)=>res.json({success:true,data:token.serializeUser(req.user)});
exports.refresh=async(req,res,next)=>{try{const raw=token.readRefreshCookie(req,AUDIENCE);if(!raw)return res.status(401).json({success:false,error:{code:'NO_TOKEN',message:'Thiếu refresh token'}});let d;try{d=token.verifyToken(raw,AUDIENCE,'refresh');}catch{return res.status(401).json({success:false,error:{code:'INVALID_TOKEN',message:'Phiên không hợp lệ hoặc đã hết hạn'}});}const hash=token.hashToken(raw);const user=await User.findById(d.id).select('+refreshSessions');const i=user?.refreshSessions?.findIndex(s=>s.tokenHash===hash&&s.audience===AUDIENCE&&s.expiresAt>Date.now())??-1;if(i<0){if(user&&d.familyId){user.refreshSessions=user.refreshSessions.filter(s=>s.familyId!==d.familyId);await user.save();}token.clearRefreshCookie(res,AUDIENCE);return res.status(401).json({success:false,error:{code:'TOKEN_REUSE',message:'Phiên đã bị thu hồi'}});}if(user.status!=='active'){user.refreshSessions.splice(i,1);await user.save();token.clearRefreshCookie(res,AUDIENCE);return res.status(403).json({success:false,error:{code:'ACCOUNT_LOCKED',message:'Tài khoản đã bị khóa'}});}const fresh=token.createRefreshToken(user,AUDIENCE,d.familyId);user.refreshSessions[i]=sessionOf(fresh,req);await user.save();token.setRefreshCookie(res,AUDIENCE,fresh.token);return res.json({success:true,data:{accessToken:token.signAccessToken(user,AUDIENCE),user:token.serializeUser(user)}});}catch(e){return next(e);}};
exports.logout=async(req,res,next)=>{try{const raw=token.readRefreshCookie(req,AUDIENCE);if(raw){try{const d=token.verifyToken(raw,AUDIENCE,'refresh');await User.updateOne({_id:d.id},{$pull:{refreshSessions:{tokenHash:token.hashToken(raw)}}});}catch{}}token.clearRefreshCookie(res,AUDIENCE);return res.json({success:true,data:null});}catch(e){return next(e);}};
exports.forgotPassword=async(req,res,next)=>{try{const {email}=forgotPasswordSchema.parse(req.body);const user=await User.findOne({email}).select('+resetPasswordToken +resetPasswordExpires');if(!user)return res.json({success:true,data:null,message:'Nếu email tồn tại, link khôi phục đã được gửi'});const raw=crypto.randomBytes(32).toString('hex');user.resetPasswordToken=crypto.createHash('sha256').update(raw).digest('hex');user.resetPasswordExpires=Date.now()+3600000;await user.save();try{await emailHelper.sendResetPasswordEmail(user.email,raw);return res.json({success:true,data:null,message:'Đã gửi email khôi phục'});}catch{user.resetPasswordToken=undefined;user.resetPasswordExpires=undefined;await user.save();return res.status(500).json({success:false,error:{code:'EMAIL_FAILED',message:'Không thể gửi email'}});}}catch(e){if(e.name==='ZodError')return res.status(400).json({success:false,error:{code:'VALIDATION_ERROR',message:e.errors[0].message}});return next(e);}};
exports.resetPassword=async(req,res,next)=>{try{const {token:raw,newPassword}=resetPasswordSchema.parse(req.body);const hash=crypto.createHash('sha256').update(raw).digest('hex');const user=await User.findOne({resetPasswordToken:hash,resetPasswordExpires:{$gt:Date.now()}}).select('+passwordHash +resetPasswordToken +resetPasswordExpires +refreshSessions');if(!user)return res.status(400).json({success:false,error:{code:'INVALID_TOKEN',message:'Token không hợp lệ hoặc đã hết hạn'}});user.passwordHash=await bcrypt.hash(newPassword,10);user.resetPasswordToken=undefined;user.resetPasswordExpires=undefined;user.refreshSessions=[];await user.save();return res.json({success:true,data:null,message:'Đổi mật khẩu thành công'});}catch(e){if(e.name==='ZodError')return res.status(400).json({success:false,error:{code:'VALIDATION_ERROR',message:e.errors[0].message}});return next(e);}};
